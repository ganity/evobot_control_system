name: Dependency Check

on:
  schedule:
    - cron: '0 0 * * 1'  # 每周一检查依赖
  workflow_dispatch:

jobs:
  check-dependencies:
    runs-on: windows-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.11"
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements-windows.txt
        pip install pip-check-reqs pipdeptree safety
    
    - name: Check for dependency conflicts
      run: |
        echo "🔍 检查依赖冲突..."
        pip check
        
        echo "📋 依赖树:"
        pipdeptree
      continue-on-error: true
    
    - name: Security audit
      run: |
        echo "🔒 安全审计..."
        safety check --json || echo "发现安全问题，请检查"
      continue-on-error: true